"""
Safar AI — Heatmap Layer (placeholder)
Can add congestion heatmap overlay if needed.
"""


def add_heatmap_layer(folium_map, scored_routes):
    """Optional: Add congestion heatmap. Currently a pass-through."""
    # Heatmap requires folium.plugins.HeatMap and dense point data.
    # For now, congestion is shown via route colors and blockage markers.
    return folium_map
